apiVersion: v1
kind: ConfigMap
metadata:
  name: pure-provisioner-config
  namepsace: {{ .Values.namespaces.k8s }}
  labels:
{{ include "pure_k8s_plugin.labels" . | indent 4}}
data:
  pure.json: |-
{{ .Values.arrays | toJson | indent 4 }}